public boolean xyBalance(String str) {
    boolean seenY = false;
    for (int i = str.length() - 1; i >= 0; i--) {
        char c = str.charAt(i);
        if (c == 'y') seenY = true;
        if (c == 'x' && !seenY) return false;
    }
    return true;
}

